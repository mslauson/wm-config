monitor=,preferred,auto,1

exec-once=$HOME/.config/hypr/autostart

source=~/.config/hypr/workspace.conf

input {
    kb_layout=
    kb_variant=ffffff
    kb_model=
    kb_options=
    kb_rules=
    numlock_by_default=true

    scroll_button=273
    follow_mouse=1

    touchpad {
        natural_scroll=true,
        disable_while_typing=true,
        tap-to-click=true,
    }
}

misc{
    
}

gestures {
    workspace_swipe=on
    workspace_swipe_fingers=3
    workspace_swipe_forever=true
    }

general {
    sensitivity=1.0 # for mouse cursor    
    gaps_in=8
    gaps_out=15
    border_size=3
    col.active_border=rgba(cba6f7ff) rgba(89b4faff) rgba(94e2d5ff) 10deg
    col.inactive_border=0xff45475a
    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)
    col.group_border=0xff89dceb
    col.group_border_active=0xfff9e2af
}

decoration {
    blur_new_optimizations = true
    drop_shadow = true
    shadow_range=100
    shadow_render_power=5
    col.shadow= 0x33000000
    col.shadow_inactive=0x22000000
    rounding=15
    blur=0
    blur_size=1 # minimum 1
    blur_passes=1 # minimum 1, more passes = more resource intensive.
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
}



animations {
    enabled=1
    # bezier=overshot,0.05,0.9,0.1,1.1
    bezier=overshot,0.13,0.99,0.29,1.1
    animation=windows,1,4,overshot,slide
    animation=border,1,10,default
    animation=fade,1,10,default
    animation=workspaces,1,6,overshot,slidevert
}

dwindle {
    pseudotile=1 # enable pseudotiling on dwindle
    force_split=0
}

master{
    
}

gestures {
    workspace_swipe=yes
    workspace_swipe_fingers=4
}

# example window rules
# for windows named/classed as abc and xyz
#windowrule=move 69 420,abc
windowrule=move center,title:^(fly_is_kitty)$
windowrule=size 800 500,title:^(fly_is_kitty)$
windowrule=move center,title:^(big_fly_kitty)$
windowrule=size 1000 1000,title:^(big_fly_kitty)$
windowrule=animation slide,title:^(all_is_kitty)$
windowrule=float,title:^(all_is_kitty)$
#windowrule=tile,xy
windowrule=tile,title:^(kitty)$
windowrule=float,title:^(fly_is_kitty)$
windowrule=float,title:^(big_fly_kitty)$
windowrule=pseudo,title:^(pseudo_is_kitty)$
windowrule=size 800 650,title:^(pseudo_is_kitty)$
windowrule=float,title:^(clock_is_kitty)$
windowrule=size 418 234,title:^(clock_is_kitty)$
windowrule=float,title:^(Steam - News)$
windowrule=size 418 234,title:^(Steam - News)$
windowrule=float,title:^(Friends List)$
windowrule=size 383 680,title:^(Friends List)$

# Message
windowrulev2 = workspace 3 silent,class:^(Signal)$
windowrulev2 = workspace 3 silent,class:^(thunderbird)
windowrulev2 = workspace 3 silent,class:^(discord)


# Gaming
windowrulev2 = workspace 5 silent,class:^(lutris)$
windowrulev2 = workspace 5 silent,class:^(Steam)$
windowrulev2 = workspace 5 silent,title:^(Friends List)$
windowrulev2 = workspace 5 silent,title:^(Steam - News)$
windowrulev2 = workspace 5 silent,class:^(steam_app)
windowrulev2 = workspace 5 silent,class:^(heroic)$

# Util
windowrulev2 = workspace 6 silent,class:^(Bitwarden)$
windowrulev2 = workspace 6 silent,title:^(notes_is_kitty)

# example binds
bindm=SUPER,mouse:272,movewindow
bindm=SUPER,mouse:273,resizewindow

bind=SUPER,t,exec,kitty --start-as=fullscreen -o 'font_size=25' --title all_is_kitty
bind=SUPER,RETURN,exec,kitty tmux
bind = SUPER_SHIFT, N, exec, kitty --title notes_is_kitty tmux attach-session -t notes
bind=CTRL_SUPER,RETURN,exec,kitty tmux attach-session
bind=SUPER_SHIFT,RETURN,exec,kitty
bind=ALT,RETURN,exec,kitty --single-instance
bind=,Print,exec,~/.config/hypr/scripts/screenshot
# bind=SUPER,RETURN,exec,alacritty
bind=SUPER,Q,killactive,
bind=SUPER_SHIFT,M,exit,
bind=SUPER,E,exec,nautilus
bind=SUPER,W,exec,firefox-developer-edition
bind=SUPER,B,exec,bitwarden-desktop
bind=SUPER,space,exec,wofi --show drun -o DP-2
bind=SUPER,D,exec,rofi -show drun

bind=SUPER,P,pseudo,
bind=SUPER,S,togglefloating,

# Screenshot binds
bind = , Print, exec, grim $(xdg-user-dir PICTURES)/screenshots/$(date +'%Y%m%d_%H%M%S.png') && notify-send --icon ~/.config/hypr/assets/square.png 'Screenshot Saved'
bind = SHIFT, Print, exec, grim -g "$(slurp)" $(xdg-user-dir PICTURES)/screenshots/$(date +'%Y%m%d_%H%M%S.png') && notify-send --icon ~/.config/hypr/assets/square.png 'Screenshot Saved'
bind = SUPER_SHIFT, Print, exec, grim -g "$(slurp)" - | swappy -f - 
bind = SUPER, Print, exec, grim - | swappy -f -
bind = CONTROL, Print, exec, grimshot copy screen && notify-send --icon ~/.config/hypr/assets/square.png "Screen copied to clipboard"
bind = SUPERCTRL, Print, exec, grim -g "$(~/bin/hyprland_get_current_window_coord.sh)" - | wl-copy -t image/png && notify-send --icon ~/.config/hypr/assets/square.png "Window copied to clipboard"
bind = SUPERCTRLSHIFT, Print, exec, grimshot copy area && notify-send --icon ~/.config/hypr/assets/square.png "Screen part copied to clipboard"

bind=SUPER_ALT,L,exec,~/.config/hypr/scripts/lock

bind=SUPER,H,movefocus,l
bind=SUPER,L,movefocus,r
bind=SUPER,K,movefocus,u
bind=SUPER,J,movefocus,d

bind=SUPERSHIFT,H,movewindow,l
bind=SUPERSHIFT,L,movewindow,r
bind=SUPERSHIFT,K,movewindow,u
bind=SUPERSHIFT,J,movewindow,d


bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9
bind=SUPER,0,workspace,10

bind=ALT,1,movetoworkspace,1
bind=ALT,2,movetoworkspace,2
bind=ALT,3,movetoworkspace,3
bind=ALT,4,movetoworkspace,4
bind=ALT,5,movetoworkspace,5
bind=ALT,6,movetoworkspace,6
bind=ALT,7,movetoworkspace,7
bind=ALT,8,movetoworkspace,8
bind=ALT,9,movetoworkspace,9
bind=ALT,0,movetoworkspace,10

bind=SUPER,mouse_down,workspace,e+1
bind=SUPER,mouse_up,workspace,e-1
bind=SUPER,tab,workspace,e+1
bind=SUPER_SHIFT,tab,workspace,e-1

bind=SUPER,g,togglegroup
bind=SUPER_ALT,tab,changegroupactive

# rec
bind=CTRL,1,exec,kitty --title fly_is_kitty --hold cava
bind=CTRL,2,exec,kitty --title fly_is_kitty --hold musikcube
bind=CTRL,3,exec,kitty --title fly_is_kitty --hold wthrr -u f
bind=CTRL_SHIFT,3,exec,kitty --title pseudo_is_kitty --hold wthrr -f w minneapolis -u f
bind=CTRL,4,exec,kitty --title clock_is_kitty --hold tty-clock -C5
bind=CTRL,5,exec,kitty --title big_fly_kitty --hold fortune -a | fmt -80 -s | $(shuf -n 1 -e cowsay cowthink) -$(shuf -n 1 -e b d g p s t w y) -f $(shuf -n 1 -e $(cowsay -l | tail -n +2)) -n | lolcat
bind=CTRL,6,exec,kitty --title big_fly_kitty  --hold fortune -a | ponysay


# Media keys
bind=,XF86AudioRaiseVolume,exec,amixer -D pipewire sset Master 5%+
# bind=,XF86AudioRaiseVolume,exec,pw-volume change +2.5%; pkill -RTMIN+8 waybar
bind=,XF86AudioLowerVolume,exec,amixer -D pipewire sset Master 5%-
# bind=,XF86AudioLowerVolume,exec,pw-volume change -2.5%; pkill -RTMIN+8 waybar
bind=,XF86AudioMute,exec,amixer -D pipewire sset Master +1 toggle
bind=,XF86AudioMute,exec,pw-volume mute toggle; pkill -RTMIN+8 waybar
# bind=,XF86AudioMute,exec,amixer -D pw-volume mute toggle
bind=,XF86MonBrightnessUp,exec,brightnessctl set +5%
bind=,XF86MonBrightnessDown,exec,brightnessctl set 5%-
bind=,XF86AudioMedia,exec,playerctl play-pause
bind=,XF86AudioPlay,exec,playerctl play-pause
bind=,XF86AudioStop,exec,playerctl stop
bind=,XF86AudioPrev,exec,playerctl previous
bind=,XF86AudioNext,exec,playerctl next

